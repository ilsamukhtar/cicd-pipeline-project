apiVersion: v1
kind: Service
metadata:
  name: flask-cicd-service
  namespace: flask-app
  labels:
    app: flask-cicd
spec:
  type: NodePort
  selector:
    app: flask-cicd
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30007
